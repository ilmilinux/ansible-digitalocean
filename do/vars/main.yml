# DO API v2 settings
do_api_token: '{{ doapikey }}'

# DO SSH key settings
do_ssh_name: wid
do_ssh_pub_key: "{{ lookup('file', 'keys/id_rsa_wid.pub') }}"
#do_ssh_private_key: "~/.ssh/id_rsa"

# DO droplet settings
do_region: sgp1
do_size: 1gb
do_image: "ubuntu-16-04-x64"
client: '{{client}}'
domain: '{{domain}}'

